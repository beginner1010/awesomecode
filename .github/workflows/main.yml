name: Triggered on push

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/setup-python@v1
    - name: Run setup and tests
      with:
        python-version: '3.7'
    - run: python setup.py develop && pytest
